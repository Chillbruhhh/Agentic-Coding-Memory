use ratatui::Frame;
use crate::app::App;

pub fn render_main_layout(_f: &mut Frame, _app: &App) {
    // This function can be used for more complex layouts
    // For now, the main UI logic is in app.rs
}
