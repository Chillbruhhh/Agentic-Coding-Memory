# TASKS

This file tracks project work items for the `amp/` subsystem.

## Done
- 2026-02-13: Fixed REST cache block read/list routes (`/v1/cache/block/read`, `/v1/cache/block/list`) and added `/v1/focus`.

## Todo
- Add integration tests (or CI smoke tests) that exercise key REST endpoints against a running server.

